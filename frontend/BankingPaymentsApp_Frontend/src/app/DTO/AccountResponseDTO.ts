export interface AccountResponseDTO {
  accountId: number;
  accountNumber: string;
  clientId: number;
  bankId:number;
  balance: number;
  accountTypeId: number;
  accountStatusId: number;
  createdAt: string; 
}
